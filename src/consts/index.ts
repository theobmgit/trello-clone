const DB_NAME = "Ifr-TrelloClone";

const DB_TABLES = {
    TASKS: "tasks",
    BOARDS: "boards",
};

const TASK_SETTINGS = {
    MAX_TASKS_PER_BOARD: 50,
    MAX_TASK_LENGTH: 500,
    MAX_BOARDS: 50,
};

export {
    DB_NAME,
    DB_TABLES,
    TASK_SETTINGS
};
