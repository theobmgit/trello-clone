import React from "react";
import BoardList from "../../components/board/BoardList";

const Home: React.FC = () => {
    return (
        <div>
            <BoardList/>
        </div>
    );
};

export default Home;
